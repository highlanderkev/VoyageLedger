---
description: Preview the Next.js project locally
---
Run this command to start the Next.js development server.

1. Start development server:
   ```bash
   npm run dev -- --hostname 0.0.0.0
   ```

Once running, the application should be available at the URL printed in the terminal (usually http://localhost:9002).
